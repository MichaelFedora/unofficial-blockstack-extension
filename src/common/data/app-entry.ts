export interface AppEntry {
  name: string;
  // displayName: string;
  description?: string;
  // version: string;
  // appIcon: { small: string, large: string }; // this would be nice too
  imageUrl: string;
  website: string;
  // launchLink: string; // I need this.. honestly
  // developer: string;
  // storageRequired?: boolean;
  // status: string;
}
